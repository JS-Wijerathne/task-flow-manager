# ─────────────────────────────────────────────
# Temp Ops Backend — Environment Configuration
# ─────────────────────────────────────────────
# Copy this file to .env and fill in your values:
#   cp .env.example .env

# ── Database ──────────────────────────────────
# Pooled connection URL (used by the application)
DATABASE_URL="postgresql://user:password@host:5432/dbname?sslmode=require"

# Direct connection URL (used for Prisma migrations)
DIRECT_URL="postgresql://user:password@host:5432/dbname?sslmode=require"

# ── Authentication ────────────────────────────
# Secret key for signing JWT tokens (min 10 chars, use a strong random string in production)
JWT_SECRET="change_me_to_a_strong_secret"
# Token expiration duration (e.g. 7d, 24h, 30m)
JWT_EXPIRES_IN="7d"

# ── Server ────────────────────────────────────
PORT=3000
# Options: development | production | test
NODE_ENV="development"
